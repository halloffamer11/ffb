# UI Demos

This folder will contain task-specific HTML pages for human validation (see `pcl/tasks.md`).

For T-001 validation, use the main dev server at `http://localhost:5173` and verify:
- Tailwind styles load (inspect classes)
- CSP header applied
- No console errors

Minimal pages or notebooks used for human validation paths.
